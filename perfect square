#include<stdio.h>
int main()
{
int num,i;
printf("enter the number");
scanf("%d",&num);
 for(i=1;i*i<=num;i++)
{
if(i*i==num)
{
printf("%d is the perfect square\n",num);
return 0;
}
}
printf("%d is mot a perfct square\n",num);
return 0;
}
